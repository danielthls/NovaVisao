unit UUtils.Consts;

interface

const
  ARQUIVO_INI           = 'EmailConfig.ini'; // Nome do arquivo INI que será criado/aberto
  SECAO_INI_CONTA_EMAIL = 'CONTA_EMAIL';     // Nome da seção do arquivo INI que contém as informações da conta de email
  CHAVE_INI_EMAIL       = 'Email';           // Nome da chave que armazena o endereço de email no arquivo INI
  CHAVE_INI_SENHA       = 'Senha';           // Nome da chave que armazena a senha no arquivo INI
  CHAVE_INI_PORTA       = 'Porta';           // Nome da chave que armazena o número da porta no arquivo INI
  CHAVE_INI_SMTP        = 'SMTP';            // Nome da chave que armazena o endereço do servidor SMTP no arquivo INI

implementation

end.
